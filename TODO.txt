5、整理存在的交互bug

2、如何支持多图选择
3、进行中，重绘disabled掉
1、默认不填写服务器，让用户自己搞定去
1、合成视频、启动中，都需要有全局loading
4、进程只给批量进行部分（每次最多5个），防止卡死
1、loading的和图片对齐，不要总是相对定位
1、重绘加全局loading

1、添加极简版登录
1、初次运行速度极慢
  是否为解压问题，如是，如何解决？
  1、替换新的打包器

1、支持导出剪映草稿格式

已经完成
1、csr放开所有connect-src
1、生成视频可以自动打开
1、合成的视频会缺少画面，找一下原因
