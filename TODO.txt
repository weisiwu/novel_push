今天任务
1、多图生成视频
1、删除后，并没有起到实际效果
  导出的视频中仍然
1、编辑字幕后，更新字幕和对应的语音以及长度

1、生成的句子不要太长
  现在常有 20多秒的句子
1、放大1.5倍没有成功，都是手动改写的
1、要识别出什么场景，人物次要，背景主要
1、已经生成过的，在自动绘图配音的时候，需要排除
  在自动生成前，不允许重绘
1、生成错误后，要继续允许生成
1、备选图中会有重复的，不知道为什么
1、可选择lora
  建议，默认不要搞lora
1、绘图提示词，变成富文本，随意编辑，不需要搞这些虚头巴脑的
=============================

1、图片生成错误的时候，测试一下
1、连续点击生图，会出现问题
  图片命名方式后续要调整
1、看这里的C:\Users\Administrator\Desktop\github\prompts\sd_prompt_generate.prompt 效果
1、生成的解说，不要调整原文
  调试
1、为什么合并字幕也这么慢
  多角色的，字幕合成很慢
  是否可以出进度条

1、支持视频帧之间的过渡
1、支持内容纯属虚构水印
1、支持用户水印
  比如正在检查sd是否可用
1、保存历史角色到本地，启动后可以加载
  sqlite
1、导出视频时候，需要如何兜底？
