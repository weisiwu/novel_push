可能需要开发的重要功能
1、不支持字幕导出和编辑
【未知】1、如何支持批量渲染

处理中
1、视频声音为什么会没有了
1、打开main页面后，默认开始链接sd，如果
  如果是空，则提示用户开始去填写
  如果ping不同，同样提示用户去写。
  两者都是一样，一键追都用不了，整体被disabled
1、初次运行速度极慢
  是否为解压问题，如是，如何解决？
  1、替换新的打包器
4、进程只给批量进行部分（每次最多5个），防止卡死
  提速 - 任务太多，导致特别卡顿
1、表格最后会多加一个图
  是否是失败后，重新多出了一个？
1、出图质量自动化
  用户自己选择 - 高质量还是中等还是低质量

1、sd是否可以多个请求并行
1、处理进度条，出现的时机太晚，需要提前
1、失败救援机制
  假设一个10分钟的视频，200个镜头，有一个失败了，如何救援，让也可以重新生成。
  比如每个图，都给一个开始时间，发现有超过20s未能绘制完毕的
1、添加极简版登录
1、支持导出剪映草稿格式


已经完成
1、csr放开所有connect-src
1、生成视频可以自动打开
1、合成的视频会缺少画面，找一下原因
5、整理存在的交互bug
1、小说转视频，隐藏掉
2、模型管理，lora管理干掉
1、系统设置干掉
1、图片展示可以更大
1、loading的和图片对齐，不要总是相对定位
1、图片预览中有保存，这个保存按钮干掉，因为有问题
  点击保存后吗，会在当前页面打开一个图片预览，丢失掉原来的数据
1、重绘加全局loading
1、选中视频后，到出列表期间，需要有个loading
1、合成视频、启动中，都需要有全局loading
3、进行中，重绘disabled掉
1、看下重绘按钮为什么失效
1、重绘没有展示loading
