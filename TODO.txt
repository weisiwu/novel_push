1、确认新流程是什么
2、有哪些关键项待调研，有什么成熟的技术方案
3、调用下国内的库，看下有没有什么比较好用的反推关键词

1、获取不超过5000字的文案，假设是一个小说
2、通过kimi获取场景和人物的prompts
3、开始生成图片
4、这里有一个vue的表格编辑组件，要求 可以增加，删除行， 行内可以编辑，能支持大量行展示
  行内数据可能会比较多（2张图）

1、视频字幕抽取
  对传入的视频，做关键帧抽取，抽取后，只获取字幕区域的图像
  https://github.com/YaoFANGUK/video-subtitle-extractor
2、tts
  专用语料库，模拟声音
============ 历史

可能需要开发的功能
1、不支持字幕导出和编辑
【未知】1、如何支持批量渲染
1、封面生成 - 隐藏掉
  意义不大，后面集成
  这个可以看看要不要也搞

处理中
1、出图慢
  sd出图速度提升
1、启动慢，如何尽量的快？
  为了快，不考虑体积
1、检测不可用后，disable没有被干掉
  仍然不能上传
1、失败救援机制
  假设一个10分钟的视频，200个镜头，有一个失败了，如何救援，让也可以重新生成。
  比如每个图，都给一个开始时间，发现有超过20s未能绘制完毕的

1、添加极简版登录
1、第一次启动后，根据系统环境，去重新生成配置
1、出图质量自动化
  用户自己选择 - 高质量还是中等还是低质量
1、支持导出剪映草稿格式
4、进程只给批量进行部分（每次最多5个），防止卡死
  提速 - 任务太多，导致特别卡顿

已经完成
1、csr放开所有connect-src
1、生成视频可以自动打开
1、合成的视频会缺少画面，找一下原因
5、整理存在的交互bug
1、小说转视频，隐藏掉
2、模型管理，lora管理干掉
1、系统设置干掉
1、图片展示可以更大
1、loading的和图片对齐，不要总是相对定位
1、图片预览中有保存，这个保存按钮干掉，因为有问题
  点击保存后吗，会在当前页面打开一个图片预览，丢失掉原来的数据
1、重绘加全局loading
1、选中视频后，到出列表期间，需要有个loading
1、合成视频、启动中，都需要有全局loading
3、进行中，重绘disabled掉
1、看下重绘按钮为什么失效
1、重绘没有展示loading
1、sdcheck失败后，界面异常
1、导出视频异常
1、打开main页面后，默认开始链接sd，如果
  如果是空，则提示用户开始去填写
  如果ping不同，同样提示用户去写。
  两者都是一样，一键追都用不了，整体被disabled
